---
import Card from "@/components/Card.astro";
import projects from "../../public/data/projects.ts";
---

<section id="projects"
  class="bg-white dark:bg-dark flex flex-col gap-12 justify-center items-center scroll-mt-16"
>
  <span class="space-y-4 text-center">
    <h2 class="">Proyectos.</h2>
    <p>
      Aquí encontrarás una muestra de mi trabajo: proyectos donde he aplicado
      mis habilidades técnicas y creativas para resolver problemas reales.
    </p>
  </span>
  <div class="w-full flex flex-wrap gap-6 justify-center xl:justify-between items-center">
    {
      projects.map((project) => (
        <Card
          title={project.title}
          description={project.description}
          badges={project.badges}
          images={project.images}
        />
      ))
    }
  </div>
</section>
